
export class Human {
    constructor(age){
        this.age = age;
        this.ourAgeNow = [];
    }

    calculateEarthYears(){
        let ourAge = this.age;
        if (ourAge === isNumber){
            return ourAge
        } else {
            return false
        }
    };
}